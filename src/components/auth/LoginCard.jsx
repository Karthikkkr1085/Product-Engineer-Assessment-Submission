import { AnimatePresence, motion, useReducedMotion } from 'framer-motion';
import { Check, Eye, EyeOff, LockKeyhole, Mail, ShieldCheck, Star } from 'lucide-react';
import { useState } from 'react';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Input } from '@/components/ui/input';
import { MotionButton } from '@/components/ui/motion-button';

const itemVariants = { hidden: { opacity: 0, y: 8 }, visible: (index) => ({ opacity: 1, y: 0, transition: { delay: 0.08 + index * 0.05, duration: 0.22, ease: 'easeOut' } }) };

export default function LoginCard({ email, password, onEmailChange, onPasswordChange, onSubmit, isSubmitting = false }) {
  const reduceMotion = useReducedMotion();
  const [showPassword, setShowPassword] = useState(false);
  const [dialog, setDialog] = useState(null);
  const motionProps = reduceMotion ? {} : { initial: 'hidden', animate: 'visible' };

  return <><motion.section {...motionProps} variants={{ hidden: { opacity: 0, y: 16, scale: 0.98 }, visible: { opacity: 1, y: 0, scale: 1, transition: { duration: 0.25, ease: 'easeOut' } } }} className="relative overflow-hidden rounded-[1.75rem] border border-white/70 bg-white/75 p-5 shadow-[0_24px_70px_rgb(15_23_42/0.14)] backdrop-blur-2xl sm:p-8"><div className="pointer-events-none absolute inset-x-8 top-0 h-px bg-linear-to-r from-transparent via-primary/30 to-transparent" /><motion.div variants={itemVariants} custom={0} className="mb-7 flex items-center gap-3"><motion.span initial={reduceMotion ? false : { opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.22 }} className="grid size-11 place-items-center rounded-2xl bg-linear-to-br from-primary to-blue-700 text-lg font-bold text-white shadow-lg shadow-primary/25">F</motion.span><div><p className="font-semibold tracking-tight text-slate-950">Finovrr</p><p className="text-xs text-slate-500">Secure loan platform</p></div></motion.div><motion.div variants={itemVariants} custom={1} className="mb-6"><h1 className="text-2xl font-semibold tracking-[-0.035em] text-slate-950 sm:text-[1.8rem]">Welcome back</h1><p className="mt-2 text-sm leading-6 text-slate-600">Continue your loan journey securely.</p></motion.div><form onSubmit={onSubmit} className="space-y-4"><motion.label variants={itemVariants} custom={2} className="group grid gap-2 text-sm font-medium text-slate-700"><span className="transition-colors group-focus-within:text-primary">Email or customer ID</span><span className="relative"><Mail className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-slate-400 transition-colors group-focus-within:text-primary" aria-hidden="true" /><Input type="text" autoComplete="username" value={email} onChange={(event) => onEmailChange(event.target.value)} className="h-11 rounded-xl border-slate-200 bg-white/80 pl-10 shadow-sm placeholder:text-slate-400 focus-visible:border-primary focus-visible:ring-4 focus-visible:ring-primary/15" placeholder="you@example.com" /></span></motion.label><motion.label variants={itemVariants} custom={3} className="group grid gap-2 text-sm font-medium text-slate-700"><span className="transition-colors group-focus-within:text-primary">Password</span><span className="relative"><LockKeyhole className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-slate-400 transition-colors group-focus-within:text-primary" aria-hidden="true" /><Input type={showPassword ? 'text' : 'password'} autoComplete="current-password" value={password} onChange={(event) => onPasswordChange(event.target.value)} className="h-11 rounded-xl border-slate-200 bg-white/80 px-10 shadow-sm placeholder:text-slate-400 focus-visible:border-primary focus-visible:ring-4 focus-visible:ring-primary/15" /><button type="button" aria-label={showPassword ? 'Hide password' : 'Show password'} onClick={() => setShowPassword((visible) => !visible)} className="absolute top-1/2 right-2 grid size-8 -translate-y-1/2 place-items-center rounded-lg text-slate-400 transition-colors hover:bg-slate-100 hover:text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30">{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></span></motion.label><motion.div variants={itemVariants} custom={4} className="flex justify-end"><button type="button" onClick={() => setDialog('forgot')} className="text-xs font-medium text-primary transition-colors hover:text-blue-700 hover:underline hover:underline-offset-4 focus-visible:rounded focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30">Forgot password?</button></motion.div><motion.div variants={itemVariants} custom={5}><MotionButton type="submit" isLoading={isSubmitting} className="h-11 w-full rounded-xl bg-linear-to-r from-primary to-blue-700 text-white shadow-lg shadow-primary/25 hover:brightness-110">Continue securely</MotionButton></motion.div></form><motion.div variants={itemVariants} custom={6} className="mt-6 border-t border-slate-200/80 pt-5"><p className="text-sm font-medium text-slate-800">New to Finovrr?</p><p className="mt-1 text-xs leading-5 text-slate-500">Track applications, save progress, and receive fast loan decisions.</p><ul className="mt-3 grid gap-1.5 text-xs text-slate-600">{['Track applications', 'Save progress securely', 'Fast loan approvals'].map((item) => <li key={item} className="flex items-center gap-2"><Check className="size-3.5 text-primary" aria-hidden="true" />{item}</li>)}</ul><button type="button" onClick={() => setDialog('create')} className="mt-4 text-sm font-semibold text-primary transition-colors hover:text-blue-700 focus-visible:rounded focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30">Create account <span aria-hidden="true">→</span></button></motion.div><motion.div variants={itemVariants} custom={7} className="mt-6 flex flex-wrap items-center gap-x-3 gap-y-2 border-t border-slate-200/80 pt-4 text-[0.7rem] font-medium text-slate-500"><span className="flex items-center gap-1.5"><ShieldCheck className="size-3.5 text-primary" aria-hidden="true" />256-bit encryption</span><span>SSL protected</span><span className="flex items-center gap-1"><Star className="size-3 fill-amber-400 text-amber-400" aria-hidden="true" />4.9 customer rating</span></motion.div></motion.section><AnimatePresence>{dialog ? <Dialog open onOpenChange={(open) => !open && setDialog(null)}><DialogContent><DialogHeader><DialogTitle>{dialog === 'create' ? 'Account registration is coming soon' : 'Password recovery is coming soon'}</DialogTitle><DialogDescription>This prototype currently supports the secure demo sign-in flow only. Use any credentials to continue.</DialogDescription></DialogHeader></DialogContent></Dialog> : null}</AnimatePresence></>;
}
